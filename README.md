# Lobster

A claude hooks thing that keeps memory of everything on your claude code session.

- Pylate-rs for local embeddings
- Grafeo for semantic serving and graph knowledge
- Redb for deterministic source-of-truth storage
- Fully rust, deterministic behavior
- "Dreaming" AI by default
- plugs on Claude Code hooks

See `ARCHITECTURE.md` for the system design.
