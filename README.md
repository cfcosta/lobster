# Lobster

A claude hooks thing that keeps memory of everything on your claude code session.

- Pylate-rs for embeddings already local
- Grafeo for graph knowledge
- Redb for key/value storage for texts and embeddings
- Fully rust, deterministic behavior
- "Dreaming" AI by default
- plugs on Claude Code hooks
